{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Controller",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceRoot}/main.go",
            "args": [
                "--kubeconfig",
                "kubeconfig"
            ]
        }
    ]
}